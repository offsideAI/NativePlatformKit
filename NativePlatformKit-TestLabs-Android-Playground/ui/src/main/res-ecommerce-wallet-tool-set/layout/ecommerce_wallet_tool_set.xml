<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="data"
            type="ai.offside.mobile.android.component.ui.tile.ecommercewallettoolset.data.EcommerceWalletToolSetData" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/ecommerce_wallet_tool_set_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/tool_set_feature_list"
            android:layout_width="@dimen/spacing.match_constraint"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:listitem="@layout/ecommerce_wallet_tool_set_tab_item"
            />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/customize_action"
            style="@style/Offside.Redesign.Button.Icon.Link"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/spacing.unset"
            android:paddingTop="@dimen/padding.medium"
            android:paddingBottom="@dimen/padding.xxlarge"
            android:textAppearance="@style/Offside.Redesign.LabelMediumTextStyle"
            android:text="@{data.customizable.label}"
            android:visibility="@{data.getCustomizableVisibility}"
            android:onClick="@{v->data.customizable.onClick(v)}"
            app:icon="@drawable/ic_actionable_edit"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tool_set_feature_list"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>